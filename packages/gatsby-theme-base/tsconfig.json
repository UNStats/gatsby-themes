{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // match output dir to input dir. e.g. dist/index instead of dist/src/index
    "rootDir": "./src"
  },
  "include": ["decs.d.ts", "src"]
}
